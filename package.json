{
  "name": "dashify-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev": "npm run dev --workspace=client",
    "client:build": "npm run build --workspace=client",
    "server:dev": "npm run dev --workspace=server",
    "server:start": "npm start --workspace=server"
  }
}
